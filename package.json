{
  "name": "solana-token-monitor-x",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Monitora contas do X (API v2 oficial) e detecta lançamentos de tokens Solana em posts, com alertas via Telegram/Discord e módulo de execução opcional (paper trading por padrão).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "shadcn": "^4.11.0"
  }
}
